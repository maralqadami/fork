const login = (user) => {
    return user;
}

const getMe = (user) => {
    return true;
}
const signUp = (user) => {
    if (user.name === 'Maral') {
        return 'Now you are in';
    } else {
        return "the username doesn't exist'
    }
};


const setComment = comment => {
    return comment 
};
